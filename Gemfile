source 'https://rubygems.org'

gem 'rails', '~> 3.2'
gem 'thin'

gem 'slim'
gem 'icalendar'

gem 'paperclip'
gem 'aws-sdk'

gem 'sqlite3', :group => [:development, :test]

group :production do
	gem 'pg'
end

group :test do
	gem 'rspec'
end

group :development do
	# To use debugger
	# gem 'debugger'
end

gem 'bootstrap-sass'
gem 'sass-rails'
gem 'coffee-rails'
gem 'uglifier', '>= 1.0.3'

gem 'jquery-rails'